# API Key Management UI — Changelog

## [Unreleased]

### Added
- "API Keys" section in Settings page for users with `canManageApiKeys` permission
- Create API key form with name and expiration date inputs
- One-click copy-to-clipboard for newly created keys with "Copied!" feedback
- Key list showing name, prefix, expiry, and created date
- Revoke key with inline confirmation prompt
- Warning message: "Save this key — it won't be shown again"
- Empty state and max keys (2) notice
